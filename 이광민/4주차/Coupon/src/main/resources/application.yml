spring:
  mvc:
    path-match:
      matching-strategy: ant_path_matcher
  profiles:
    active: prod
    include:
      - API-KEY
springdoc:
  paths-to-exclude: /swagger-resources/**
server:
  port: 8080
