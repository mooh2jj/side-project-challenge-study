package com.study.sidejavachallenge.domain.board.repository;

import com.study.sidejavachallenge.domain.board.entity.CategoryBridge;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CategoryBridgeRepository extends JpaRepository<CategoryBridge, Long> {
}
